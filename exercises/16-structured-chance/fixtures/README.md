# Section 16 fixtures

This section does not need an external fixture file. Its known clipping case,
seed replay, boundary checks, and hierarchy properties are explicit in
`tests/structured_chance_model_test.cpp`. Add a small CC0-1.0 TSV fixture here
only if your own grammar needs saved input beyond those named cases.
