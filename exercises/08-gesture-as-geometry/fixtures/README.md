# Independent gesture oracle

`gesture-oracle.txt` was hand-calculated for `k = log(2)`, one-second samples,
and no filtering: each smoothed point moves halfway to its raw target. Rows have
exactly nine fields and are parsed strictly by the public test.
