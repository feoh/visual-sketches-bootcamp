# Starter

Edit `src/design/gesture_design.cpp` to own filtering, smoothing, width mapping,
and palette. Then replace the connected ribbon with your own geometric mark
language. Drag with a pointer or use arrow keys; press C to clear.
