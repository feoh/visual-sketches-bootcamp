# Starter

Change parameters in `src/design/family_design.cpp`, then replace the circle-and-vertical-tick rendering in `src/ofApp.cpp` with learner-owned repeated geometry. Keep the public model API intact.
