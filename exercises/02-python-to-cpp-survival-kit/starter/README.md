# Starter

1. Change the repeated-mark values in `src/design/family_design.cpp`.
2. Replace the circles and vertical ticks in `src/ofApp.cpp` with your own marks.
3. Keep the function names and declarations in `shared/` unchanged because the
   tests call them directly.
