# Starter

`learner_known_case.cpp` is deliberately green. Replace its arranged seed and expected values with your own independently reviewed case while preserving the act and assertion structure. The injected model factory exists so the public contract can prove your test rejects a controlled wrong implementation; always call it exactly once in the act step.
