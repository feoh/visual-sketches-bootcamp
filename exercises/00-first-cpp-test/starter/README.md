# Starter

1. Edit `learner_known_case.cpp` and choose one seed whose expected numbers you
   worked out separately.
2. Keep the three small steps: make the input, run the model once, and compare
   its answer with yours.
3. Leave the supplied model-factory function alone. The tests use it to confirm
   that your test rejects a deliberately wrong answer.
