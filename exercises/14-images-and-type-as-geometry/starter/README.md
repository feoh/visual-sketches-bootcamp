# Starter: mask constellation

This adapter turns dark pixels in the bundled original mask into circles and
moves the complete geometry with a small rotation and vertical oscillation.
Start in `src/design/image_geometry_design.cpp`; it is the learner-owned seam.
The shared model must stay platform-neutral. See the parent README for exact
commands, asset replacement, licensing, controls, and manual checks.
