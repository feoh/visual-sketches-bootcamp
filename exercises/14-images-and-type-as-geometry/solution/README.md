# Explained reference: negative-space ribbons

## What this example tries

This is intentionally visually divergent from the starter. It samples bright negative
space at a coarser density, rotates the cloud, and draws long phased bars rather than
dark-pixel circles. The same checked model, asset, controls, and failure behavior
support both looks; those are the transferable parts. This is one explanation, not an
official finished target.
