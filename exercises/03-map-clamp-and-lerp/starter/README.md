# Starter

1. Change the poster values in `src/design/poster_design.cpp`.
2. Replace the panel, dot, and line treatment in `src/ofApp.cpp` with your own
   responsive layout.
3. Keep the function names and declarations in `shared/` unchanged because the
   tests call them directly.
