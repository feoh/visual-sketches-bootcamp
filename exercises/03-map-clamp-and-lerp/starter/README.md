# Starter

Change `src/design/poster_design.cpp`, then replace the filled-panel, dot, and rule treatment in `src/ofApp.cpp` with learner-owned responsive geometry and hierarchy. Keep the public model API intact.
