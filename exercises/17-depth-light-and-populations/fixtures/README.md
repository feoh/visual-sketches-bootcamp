# Section 17 fixtures

This section does not need an external fixture file. Seed replay and explicit
cluster/transform properties are checked directly in
`tests/population_model_test.cpp`. Add a small CC0-1.0 fixture here only if your
own population uses recorded controls or cluster data that should survive a
code change.
