# Deterministic fixtures

`viewports.tsv` contains explicit width/height pairs used by the property test.
It is original course data, licensed under the repository license, and contains
no device, clock, random, renderer, or platform input.
