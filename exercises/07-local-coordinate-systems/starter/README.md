# Starter: linked rods and rings

Change `src/design/sculpture_design.cpp`, then replace the line, dot, and ring
language in `src/ofApp.cpp`. Keep exactly three phases, local geometry, nested
scoped transforms, responsive bounds, and the public model contract.
