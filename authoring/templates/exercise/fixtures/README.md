# Deterministic fixtures

Document each fixture's format, origin, creator, license, and expected failure
behavior. Never require a live device or network response for deterministic
tests. Recorded camera/audio fixtures must respect consent and retention rules.
