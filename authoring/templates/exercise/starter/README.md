# Starter project

Track canonical `src/`, `addons.make`, and `bin/data/` inputs only. State each
intentional completion gap. Project Generator owns native metadata, which must
not be copied into this template.
