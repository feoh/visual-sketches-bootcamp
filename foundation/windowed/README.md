# Windowed foundation probe

This adapter renders the deterministic model from `shared/core` and loads the
bundled CC0 `bin/data/foundation-probe.ppm` asset. Generate and build it with the
repository wrappers; run the executable manually on a host with a display.
